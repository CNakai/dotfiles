general {
    output_format = "i3bar"
    colors = true
    interval = 5
}

order += "wireless _first_"
order += "cpu_usage"
order += "disk /"
order += "battery 0"
order += "tztime local"

disk "/" {
    format = "%avail "
}

wireless _first_ {
    format_up = " %quality at %essid %ip "
    format_down = " ☡ WiFi "
}

battery 0 {
    format = " %status %percentage "
    low_threshold = 15
    threshold_type = time
}

cpu_usage {
    format = " CPU: %usage "
}

tztime local{
    format = " %a, %d %b %Y %r "
}
